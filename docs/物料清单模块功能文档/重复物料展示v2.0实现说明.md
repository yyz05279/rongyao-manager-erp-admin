# 重复物料展示 v2.0 实现说明

## 更新日期

2025-10-28

## 更新背景

根据后端优化的数据结构（详见 `物料清单重复处理优化开发完成报告.md`），前端需要相应调整 UI 展示，实现更友好的重复物料明细展示。

## 核心改进

### 从 v1.0 到 v2.0 的变化

#### v1.0 数据结构（扁平列表）

```json
{
  "skippedRecords": 3,
  "duplicateItems": [
    {
      "materialName": "三通",
      "specification": "配套连接片...",
      "quantity": 1,
      "rowNumber": 46
    },
    {
      "materialName": "三通",
      "specification": "配套连接片...",
      "quantity": 2,
      "rowNumber": 58
    }
  ]
}
```

**问题**：

- 无法区分哪些重复项属于同一个已存在物料
- 缺少数据库中已存在物料的完整信息
- 用户需要手动计算累加数量

#### v2.0 数据结构（分组展示）

```json
{
  "skippedRecords": 3,
  "existedItems": [
    {
      "existedItem": {
        "id": 123,
        "materialName": "三通",
        "specification": "配套连接片...",
        "quantity": 5,
        "unit": "套",
        "sheetName": "电控设备汇总",
        "itemCode": "ITEM001"
      },
      "duplicateItems": [
        {
          "materialName": "三通",
          "specification": "配套连接片...",
          "quantity": 1,
          "rowNumber": 46
        },
        {
          "materialName": "三通",
          "specification": "配套连接片...",
          "quantity": 2,
          "rowNumber": 58
        }
      ]
    }
  ]
}
```

**优势**：

- 按已存在物料分组，结构清晰
- 包含完整的数据库物料信息
- 自动计算累加后的数量
- 支持分批上传的数据合并

## 实现方案

### 1. 类型定义更新

#### 新增类型（`src/api/erp/saltprocess/material/types.ts`）

```typescript
/**
 * 已存在物料信息（v2.0）
 */
export interface ExistedItemVo {
  existedItem: ExistedItemDetailVo; // 数据库中已存在的物料
  duplicateItems: DuplicateMaterialItem[]; // 本次上传中与该物料重复的所有记录
}

/**
 * 已存在物料详细信息
 */
export interface ExistedItemDetailVo {
  id: number;
  materialName: string;
  specification?: string;
  quantity: number;
  unit?: string;
  sheetName?: string;
  itemCode?: string;
  equipmentType?: string;
}
```

#### 更新导入结果类型

```typescript
export interface MaterialImportResultVo {
  // ... 其他字段
  existedItems?: ExistedItemVo[]; // 已存在物料信息（v2.0新结构）
  duplicateItems?: DuplicateMaterialItem[]; // 重复物料明细（兼容v1.0）
}
```

### 2. 数据收集逻辑

由于是分批上传，需要在本地合并多个批次的重复数据。

#### 使用 Map 收集数据

```typescript
// 使用 Map 收集和合并所有批次的重复物料
// key 为物料的唯一标识（materialName + specification）
const existedItemsMap = new Map<string, any>();
```

#### 收集每个批次的数据

```typescript
if (result.existedItems && result.existedItems.length > 0) {
  // v2.0 新结构：按已存在物料分组
  result.existedItems.forEach((existedItemVo: any) => {
    const existedItem = existedItemVo.existedItem;
    const materialKey = `${existedItem.materialName}_${existedItem.specification || ''}`;

    if (existedItemsMap.has(materialKey)) {
      // 合并重复项到现有物料
      const existing = existedItemsMap.get(materialKey);
      existing.duplicateItems.push(...existedItemVo.duplicateItems);
    } else {
      // 新增物料
      existedItemsMap.set(materialKey, {
        existedItem: existedItem,
        duplicateItems: [...existedItemVo.duplicateItems]
      });
    }
  });
}
```

#### 兼容 v1.0 数据结构

```typescript
else if (result.duplicateItems && result.duplicateItems.length > 0) {
  // v1.0 兼容：扁平结构，需要手动分组（降级处理）
  result.duplicateItems.forEach((duplicateItem: any) => {
    const materialKey = `${duplicateItem.materialName}_${duplicateItem.specification || ''}`;
    if (!existedItemsMap.has(materialKey)) {
      existedItemsMap.set(materialKey, {
        existedItem: {
          materialName: duplicateItem.materialName,
          specification: duplicateItem.specification,
          quantity: 0, // 旧结构没有数据库数量信息
          unit: duplicateItem.unit
        },
        duplicateItems: []
      });
    }
    existedItemsMap.get(materialKey).duplicateItems.push(duplicateItem);
  });
}
```

#### 转换为数组

```typescript
// 转换 Map 为数组
const existedItemsList = Array.from(existedItemsMap.values());

// 设置到结果中
importResult.value = {
  // ... 其他字段
  existedItems: existedItemsList.length > 0 ? existedItemsList : undefined
};
```

### 3. UI 展示优化

#### 嵌套展示结构

```vue
<!-- 第一行：已存在的物料信息 -->
<div class="existed-item-header">
  <el-tag type="primary" size="small">1</el-tag>
  <el-tag type="success" effect="plain" size="small">已存在</el-tag>
  <span class="material-name">三通</span>
  <span class="material-spec">（配套连接片，螺丝螺母，盖板）</span>
  <span class="material-info">
    - 数据库数量：
    <el-tag type="info" size="small">5 套</el-tag>
  </span>
  <span class="material-info">- 物品编码：ITEM001</span>
  <span class="duplicate-count">（本次重复上传 2 次）</span>
</div>

<!-- 换行缩进：重复项列表 -->
<div class="duplicate-items-list">
  <el-table :data="duplicateItems" style="margin-left: 40px;">
    <el-table-column type="index" label="重复序号" />
    <el-table-column prop="quantity" label="上传数量" />
    <el-table-column prop="sheetName" label="来源Sheet" />
    <el-table-column prop="rowNumber" label="Excel行号" />
  </el-table>

  <!-- 累加提示 -->
  <div class="accumulate-tip">
    建议操作：将这 2 次上传的数量（共 3 套）累加到数据库现有数量，更新为 8 套
  </div>
</div>
```

#### 视觉层次

1. **已存在物料** - 蓝色边框，浅蓝背景

   - 物料名称加粗
   - 数据库数量用标签高亮
   - 重复次数用警告色标注

2. **重复项列表** - 左侧缩进 40px

   - 表格展示每次上传的详情
   - 重复序号用警告标签
   - 数量字段加粗

3. **累加提示** - 灰色背景，左侧边框
   - 使用信息图标
   - 关键数字用蓝色加粗

### 4. 样式设计

```scss
.existed-item-group {
  margin-bottom: 20px;
  padding: 12px;
  background-color: #fafafa;
  border-radius: 4px;
  border: 1px solid #e4e7ed;

  .existed-item-header {
    padding: 10px;
    background-color: #f0f9ff;
    border-radius: 4px;
    border-left: 4px solid #409eff;
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 4px;

    .material-name {
      font-size: 14px;
      font-weight: 600;
      color: #303133;
    }

    .duplicate-count {
      font-size: 12px;
      color: #e6a23c;
      font-weight: 600;
    }
  }

  .duplicate-items-list {
    margin-top: 12px;

    .accumulate-tip {
      margin: 12px 0 0 40px;
      padding: 10px 12px;
      background-color: #f4f4f5;
      border-left: 3px solid #909399;
      border-radius: 4px;
      font-size: 13px;
      color: #606266;

      strong {
        color: #409eff;
        font-weight: 600;
      }
    }
  }
}
```

## 效果展示

### 修改前（v1.0）

**扁平列表展示**：

| 序号 | 物料名称 | 规格型号      | 数量 | Excel 行号 | 原因 |
| ---- | -------- | ------------- | ---- | ---------- | ---- |
| 1    | 三通     | 配套连接片... | 1    | 第 46 行   | 重复 |
| 2    | 三通     | 配套连接片... | 2    | 第 58 行   | 重复 |
| 3    | 法兰     | DN50 PN16     | 3    | 第 60 行   | 重复 |

**问题**：

- 看不出哪些是同一个物料的重复
- 不知道数据库中已有多少数量
- 需要手动计算累加后的数量

### 修改后（v2.0）

**分组嵌套展示**：

#### 物料 1：三通

📌 **已存在** 三通（配套连接片，螺丝螺母，盖板） - 数据库数量：5 套 - 物品编码：ITEM001 **（本次重复上传 2 次）**

| 重复序号 | 上传数量 | 来源 Sheet   | Excel 行号 |
| -------- | -------- | ------------ | ---------- |
| 第 1 次  | 1 套     | 电控设备汇总 | 第 46 行   |
| 第 2 次  | 2 套     | 电控设备汇总 | 第 58 行   |

💡 **建议操作**：将这 2 次上传的数量（共 **3 套**）累加到数据库现有数量，更新为 **8 套**

#### 物料 2：法兰

📌 **已存在** 法兰（DN50 PN16） - 数据库数量：10 个 - 物品编码：ITEM002 **（本次重复上传 1 次）**

| 重复序号 | 上传数量 | 来源 Sheet   | Excel 行号 |
| -------- | -------- | ------------ | ---------- |
| 第 1 次  | 3 个     | 机械设备汇总 | 第 60 行   |

💡 **建议操作**：将这 1 次上传的数量（共 **3 个**）累加到数据库现有数量，更新为 **13 个**

**优势**：

- ✅ 按物料分组，一目了然
- ✅ 显示数据库现有数量
- ✅ 自动计算累加后的数量
- ✅ 提供明确的操作建议

## 分批上传的数据合并

### 场景说明

用户上传物料清单时，由于数据量大，会分多个批次上传：

- 第 1 批：50 条物料，其中"三通"重复 1 次
- 第 2 批：50 条物料，其中"三通"重复 2 次
- 第 3 批：50 条物料，其中"法兰"重复 1 次

### 合并逻辑

使用 Map 以物料唯一标识（materialName + specification）为 key 进行合并：

```typescript
批次1返回: existedItems = [{ existedItem: {三通, qty:5}, duplicateItems: [1次] }]
  ↓ 存入 Map
existedItemsMap["三通_配套连接片"] = { existedItem: {三通, qty:5}, duplicateItems: [1次] }

批次2返回: existedItems = [{ existedItem: {三通, qty:5}, duplicateItems: [2次] }]
  ↓ 合并到 Map
existedItemsMap["三通_配套连接片"] = { existedItem: {三通, qty:5}, duplicateItems: [1次, 2次, 2次] }

批次3返回: existedItems = [{ existedItem: {法兰, qty:10}, duplicateItems: [1次] }]
  ↓ 存入 Map
existedItemsMap["法兰_DN50"] = { existedItem: {法兰, qty:10}, duplicateItems: [1次] }

全部完成后转换为数组:
existedItemsList = [
  { existedItem: {三通, qty:5}, duplicateItems: [1次, 2次, 2次] },  // 3次重复
  { existedItem: {法兰, qty:10}, duplicateItems: [1次] }           // 1次重复
]
```

### 最终展示

**重复物料明细（共 4 条重复，涉及 2 个物料）：**

1. **三通** - 本次重复上传 3 次
2. **法兰** - 本次重复上传 1 次

## 兼容性说明

### 向后兼容 v1.0

如果后端返回的是旧结构（duplicateItems），前端会：

1. 检测到 `result.duplicateItems` 存在
2. 手动构建 `existedItem`（数量设为 0，因为旧结构没有此信息）
3. 按物料名称+规格分组
4. 正常展示（但缺少数据库数量和物品编码）

### 优先使用 v2.0

```typescript
if (result.existedItems && result.existedItems.length > 0) {
  // 使用新结构
} else if (result.duplicateItems && result.duplicateItems.length > 0) {
  // 降级到旧结构
}
```

## 测试验证

### 场景 1：单批次单物料重复

**输入**：

```json
{
  "existedItems": [{
    "existedItem": { "materialName": "三通", "quantity": 5, "unit": "套" },
    "duplicateItems": [
      { "quantity": 1, "rowNumber": 46 },
      { "quantity": 2, "rowNumber": 58 }
    ]
  }]
}
```

**预期**：

- 显示 1 个物料组
- 重复 2 次
- 累加建议：5 + 3 = 8 套

### 场景 2：多批次同一物料

**批次 1**：

```json
{ "existedItems": [{ "existedItem": {...}, "duplicateItems": [1次] }] }
```

**批次 2**：

```json
{ "existedItems": [{ "existedItem": {...}, "duplicateItems": [2次] }] }
```

**预期**：

- 最终合并为 1 个物料组
- 重复 3 次（批次 1 的 1 次 + 批次 2 的 2 次）

### 场景 3：多个物料重复

**预期**：

- 显示多个物料组
- 每个物料独立计算累加数量

## 文件修改清单

### 修改文件

| 文件路径                                                          | 修改内容                                     |
| ----------------------------------------------------------------- | -------------------------------------------- |
| `src/api/erp/saltprocess/material/types.ts`                       | 新增 ExistedItemVo、ExistedItemDetailVo 类型 |
| `src/views/erp/saltprocess/project/components/MaterialDetail.vue` | 更新数据收集逻辑和 UI 展示                   |

## 后续优化建议

1. **批量更新按钮**：在每个物料组添加"累加数量"按钮，自动调用更新接口
2. **全部累加**：添加"全部累加"按钮，一键处理所有重复物料
3. **忽略重复**：添加"忽略"选项，跳过某些重复物料
4. **导出报告**：支持导出重复物料明细报告

## 总结

v2.0 的重复物料展示优化：

| 对比项   | v1.0       | v2.0                 |
| -------- | ---------- | -------------------- |
| 数据结构 | 扁平列表   | 分组嵌套             |
| 物料信息 | 不完整     | 完整（含数据库信息） |
| 分批合并 | 不支持     | 自动合并             |
| 累加计算 | 需手动     | 自动计算并提示       |
| 用户体验 | 需额外思考 | 一目了然             |

---

**文档版本**：v2.0  
**编写日期**：2025-10-28  
**编写人员**：AI Assistant
