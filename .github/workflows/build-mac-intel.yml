name: Build macOS Intel (x86_64)

on:
  # 已禁用 - 仅在需要时手动触发
  # workflow_call:
  # push:
  #   tags:
  #     - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    strategy:
      matrix:
        node-version: [16.x]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        shell: bash
        run: |
          if [[ -f "package-lock.json" || -f "npm-shrinkwrap.json" ]]; then
            echo "Using npm ci (lockfile found)"
            npm ci
          elif [[ -f "yarn.lock" ]]; then
            echo "Using yarn (yarn.lock found)"
            corepack enable || true
            yarn install --frozen-lockfile
          else
            echo "No lockfile found, falling back to npm install"
            npm install
          fi
      
      - name: Build Vue application
        run: npm run build
      
      - name: Build Electron app (Intel x86_64)
        env:
          CSC_IDENTITY_AUTO: "false"  # Skip code signing for CI test builds
        run: |
          # Ensure no accidental signing variables leak in
          unset APPLE_ID APPLE_PASSWORD APPLE_TEAM_ID CSC_LINK CSC_KEY_PASSWORD || true
          npm run electron:build -- --mac --x64 --publish=never
      
      - name: List build output
        run: |
          ls -lh dist/
          file dist/*.dmg || true
      
      - name: Verify build output
        run: |
          if [ ! -d "dist" ]; then
            echo "❌ Error: dist directory not found"
            exit 1
          fi

          # 查找所有 .dmg 文件（支持中文和英文文件名）
          dmgFiles=$(find dist -maxdepth 1 -name "*.dmg" -type f 2>/dev/null | wc -l)
          if [ "$dmgFiles" -eq 0 ]; then
            echo "❌ Error: No DMG files found in dist directory"
            echo "Available files:"
            ls -lh dist/ || true
            exit 1
          fi

          echo "✓ Build output verified"
          echo "Found $dmgFiles DMG file(s)"
          find dist -maxdepth 1 -name "*.dmg" -type f -exec ls -lh {} \;

      - name: Create checksums
        run: |
          cd dist

          # 查找所有 .dmg 文件并计算校验和
          dmgFiles=$(find . -maxdepth 1 -name "*.dmg" -type f)
          if [ -z "$dmgFiles" ]; then
            echo "❌ Error: No DMG files found for checksum calculation"
            exit 1
          fi

          # 为所有 DMG 文件生成校验和
          echo "$dmgFiles" | while read dmgFile; do
            shasum -a 256 "$dmgFile"
          done > checksums-intel.txt

          if [ ! -s checksums-intel.txt ]; then
            echo "❌ Error: Failed to create checksums"
            exit 1
          fi

          echo "✓ Checksums created successfully"
          cat checksums-intel.txt

      - name: Upload macOS Intel artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-intel
          path: |
            dist/*.dmg
            dist/*.zip
            dist/checksums-intel.txt
          retention-days: 30
          if-no-files-found: warn

